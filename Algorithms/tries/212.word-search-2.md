## [212 Word Search II](https://leetcode.com/problems/word-search-ii/description/)

<!-- notecardId: 1767892875031 -->

```js
// Explanation:
// - Neetcode: https://youtu.be/asbcE9mZz_U

class TrieNode {
  #children = new Map();

  word = null;

  getChild(key) {
    return this.#children.get(key);
  }

  getOrCreateChild(char) {
    let child = this.#children.get(char);

    if (!child) {
      child = new TrieNode();
      this.#children.set(char, child);
    }

    return child;
  }
}

class Trie {
  root = new TrieNode();

  insert(word) {
    let node = this.root;

    for (const char of word) {
      node = node.getOrCreateChild(char);
    }

    node.word = word;
  }
}

function findWords(board, words) {
  const trie = new Trie();

  for (const word of words) {
    trie.insert(word);
  }

  const rows = board.length;
  const cols = board[0].length;
  const result = [];

  for (let r = 0; r < rows; r += 1) {
    for (let c = 0; c < cols; c += 1) {
      backtrack(r, c, trie.root);
    }
  }

  return result;

  function backtrack(row, col, prevNode) {
    if (
      row < 0 ||
      col < 0 ||
      row === rows ||
      col === cols ||
      board[row][col] === '#'
    ) {
      return;
    }

    const char = board[row][col];
    const node = prevNode.getChild(char);
    if (!node) return;

    if (node.word) {
      result.push(node.word);
      node.word = null;
    }

    board[row][col] = '#';

    backtrack(row - 1, col, node);
    backtrack(row, col + 1, node);
    backtrack(row + 1, col, node);
    backtrack(row, col - 1, node);

    board[row][col] = char;
  }
}
```
