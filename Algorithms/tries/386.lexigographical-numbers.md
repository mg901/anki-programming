## [386 Lexicographical Numbers 'dfs'](https://leetcode.com/problems/lexicographical-numbers/description/)

<!-- notecardId: 1765737924882 -->

```js
// Explanation:
//  - Neetcode: https://youtu.be/QihtII-FvLo

// - Time: O(n)
// - Space: O(h)
function lexicalOrder(n) {
  const result = [];

  for (let i = 1; i <= 9; i += 1) {
    generate(i, n, result);
  }

  return result;
}

function generate(current, limit, result) {
  if (current > limit) return;

  result.push(current);

  for (let digit = 0; digit <= 9; digit += 1) {
    const next = current * 10 + digit;
    if (next > limit) break;

    generate(next, limit, result);
  }
}
```

## [386 Lexicographical Numbers 'space-optimized'](https://leetcode.com/problems/lexicographical-numbers/description/)

<!-- notecardId: 1765739833555 -->

```js
// Explanation:
//  - Neetcode: https://youtu.be/QihtII-FvLo

// - Time: O(n)
// - Space: O(1)
function lexicalOrder(n) {
  const RADIX = 10;
  const result = [];
  let current = 1;

  for (let i = 0; i < n; i += 1) {
    result.push(current);

    if (current * 10 <= n) {
      current *= RADIX;
    } else {
      while (current % RADIX === 9 || current + 1 > n) {
        current = Math.trunc(current / RADIX);
      }

      current += 1;
    }
  }

  return result;
}
```
