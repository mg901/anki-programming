## [36 Valid Sudoku](https://leetcode.com/problems/valid-sudoku/description/)

<!-- notecardId: 1759018712492 -->

```js
// Explanation:
// - Neetcode: https://youtu.be/TjFXEUCMqI8

// - Time: O(n^2)
// - Space: O(n^2)
function isValidSudoku(board) {
  const BOARD_SIZE = 9;
  const SUB_BOARD_SIZE = 3;
  const rows = Array.from({ length: BOARD_SIZE }, () => new Set());
  const cols = Array.from({ length: BOARD_SIZE }, () => new Set());
  const subBoard = Array.from({ length: BOARD_SIZE }, () => new Set());

  for (let r = 0; r < BOARD_SIZE; r += 1) {
    for (let c = 0; c < BOARD_SIZE; c += 1) {
      const value = board[r][c];
      if (value === '.') continue;

      const rowBlock = Math.floor(r / SUB_BOARD_SIZE);
      const colBlock = Math.floor(c / SUB_BOARD_SIZE);
      const subIndex = rowBlock * SUB_BOARD_SIZE + colBlock;

      if (
        rows[r].has(value) ||
        cols[c].has(value) ||
        subBoard[subIndex].has(value)
      ) {
        return false;
      }

      rows[r].add(value);
      cols[c].add(value);
      subBoard[subIndex].add(value);
    }
  }

  return true;
}
```
