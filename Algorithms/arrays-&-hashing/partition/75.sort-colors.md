## [75 Sort Colors](https://leetcode.com/problems/sort-colors/description/)

<!-- notecardId: 1767978896141 -->

```js
// Explanation:
// - Neetcode: https://youtu.be/4xbWSRZHqac

// Pattern: Two Pointers

// Sub-pattern: Slow / Fast
//    Technique: Partitioning (Dutch National Flag / 3-way)
//        Specific: Unstable Partition

// - Time: O(n)
// - Space: O(1)
function sortColors(nums) {
  let low = 0;
  let mid = 0;
  let high = nums.length - 1;

  while (mid <= high) {
    if (nums[mid] === 0) {
      swap(nums, mid, low);
      low += 1;
      mid += 1;
    } else if (nums[mid] === 1) {
      mid += 1;
    } else {
      swap(nums, mid, high);
      high -= 1;
    }
  }
}

function swap(arr, i, j) {
  if (i === j) return;

  const temp = arr[j];
  arr[j] = arr[i];
  arr[i] = temp;
}
```
