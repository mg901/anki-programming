## [151 Reverse Words in String](https://leetcode.com/problems/reverse-words-in-a-string/description/)

<!-- notecardId: 1765862737149 -->

```js
// - Time: O(n)
// - Space: O(n)
function reverseWords(s) {
  const n = s.length;
  const words = s.split(' ');
  const result = [];

  for (let i = n - 1; i >= 0; i -= 1) {
    if (words[i]) {
      result.push(words[i]);
    }
  }

  return result.join(' ');
}
```

## [151 Reverse Words in String 'Two-Pointer backward scan'](https://leetcode.com/problems/reverse-words-in-a-string/description/)

<!-- notecardId: 1767350191056 -->

```js
// - Time: O(n)
// - Space: O(n)
function reverseWords(s) {
  const SPACE = ' ';
  let right = s.length - 1;
  let left = right;
  const result = [];

  while (left >= 0) {
    while (left >= 0 && s[left] === SPACE) {
      left -= 1;
    }

    if (left < 0) break;
    right = left;

    while (left >= 0 && s[left] !== SPACE) {
      left -= 1;
    }

    result.push(s.slice(left + 1, right + 1));
  }

  return result.join(' ');
}
```
