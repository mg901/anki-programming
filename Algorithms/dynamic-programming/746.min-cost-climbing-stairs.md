## [746 Min Cost Climbing Stairs](https://leetcode.com/problems/min-cost-climbing-stairs/description/)

<!-- notecardId: 1753097013414 -->

```js
// Top-down ------------
function minCostClimbingStairs(cost) {
  const cache = new Int32Array(cost.length).fill(-1);

  return Math.min(dfs(0), dfs(1));

  function dfs(index) {
    if (index >= cost.length) return 0;
    if (cache[index] !== -1) return cache[index];

    cache[index] = cost[index] + Math.min(dfs(index + 1), dfs(index + 2));

    return cache[index];
  }
}

// Bottom-up ------------
function minCostClimbingStairs(cost) {
  const n = cost.length;
  const dp = new Array(n + 1).fill(0);

  for (let i = 2; i <= n; i += 1) {
    dp[i] = Math.min(dp[i - 1] + cost[i - 1], dp[i - 2] + cost[i - 2]);
  }

  return dp[n];
}

// Space Optimized ------------
function minCostClimbingStairs(cost) {
  let prev = 0;
  let current = 0;

  for (let i = 2; i <= cost.length; i += 1) {
    const next = Math.min(current + cost[i - 1], prev + cost[i - 2]);
    prev = current;
    current = next;
  }

  return current;
}
```
