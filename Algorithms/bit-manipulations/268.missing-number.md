## [268 Missing Number 'xor'](https://leetcode.com/problems/missing-number/description/)

<!-- notecardId: 1762779377050 -->

```js
// Explanation:
// - Neetcode: https://youtu.be/WnPLSRLSANE

// - Time: O(n)
// - Space: O(1)
function missingNumber(nums) {
  const n = nums.length;
  let xor = n;

  for (let i = 0; i < n; i += 1) {
    xor ^= nums[i] ^ i;
  }

  return xor;
}
```

## [268 Missing Number 'binary-search'](https://leetcode.com/problems/missing-number/description/)

<!-- notecardId: 1762864536692 -->

```js
// Explanation:
// - Neetcode: https://youtu.be/WnPLSRLSANE

// - Time: O(n log n)
// - Space: O(1)
function missingNumber(nums) {
  nums.sort((a, b) => a - b);

  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    const mid = left + ((right - left) >> 1);

    if (mid !== nums[mid]) {
      right = mid - 1;
    } else {
      left = mid + 1;
    }
  }

  return left;
}
```
