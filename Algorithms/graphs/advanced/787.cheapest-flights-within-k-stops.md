## [787 Cheapest Flights Within K Stops 'Bellman-Ford'](https://leetcode.com/problems/cheapest-flights-within-k-stops/description/)

```js
function findCheapestPrice(n, flights, src, dst, k) {
  let prices = new Array(n).fill(Infinity);
  prices[src] = 0;

  for (let i = 0; i <= k; i += 1) {
    const temp = [...prices];

    for (const [u, v, weight] of flights) {
      if (prices[u] !== Infinity && prices[u] + weight < temp[v]) {
        temp[v] = prices[u] + weight;
      }
    }

    prices = temp;
  }

  return prices[dst] === Infinity ? -1 : prices[dst];
}
```
