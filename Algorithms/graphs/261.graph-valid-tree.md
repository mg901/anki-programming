## [261 Graph Valid Tree 'dfs'](https://neetcode.io/problems/valid-tree?list=neetcode150)

```js
// Explanation:
// - Neetcode: https://youtu.be/bXsUuownnoQ

// Complexity:
// - Time: O(V + E)
// - Space: O(V + E)
class Solution {
  validTree(n, edges) {
    if (edges.length !== n - 1) return false;

    const graph = createGraph(n, edges);
    const visited = new Set();

    if (!dfs(0, -1)) return false;

    return visited.size === n;

    function dfs(u, parent) {
      if (visited.has(u)) return false;

      visited.add(u);

      for (const v of graph[u]) {
        if (v === parent) continue;
        if (!dfs(v, u)) return false;
      }

      return true;
    }
  }
}

function createGraph(n, edges) {
  const graph = Array.from({ length: n }, () => []);

  for (const [u, v] of edges) {
    graph[u].push(v);
    graph[v].push(u);
  }

  return graph;
}
```

## [261 Graph Valid Tree 'union find'](https://neetcode.io/problems/valid-tree?list=neetcode150)

```js
class DisjointSet {
  constructor(size) {
    this.parent = Array.from({ length: size }, (_, i) => i);
    this.rank = new Unit32Array(size);
  }

  find(x) {
    if (this.parent[x] !== x) {
      this.parent[x] = this.find(this.parent[x]);
    }

    return this.parent[x];
  }

  union(a, b) {
    const rootA = this.find(a);
    const rootB = this.find(b);

    if (rootA === rootB) return false;

    if (this.rank[rootA] < this.rank[rootB]) {
      this.parent[rootA] = rootB;
    } else if (this.rank[rootA] > this.rank[rootB]) {
      this.parent[rootB] = rootA;
    } else {
      this.parent[rootB] = rootA;
      this.rank[rootA] += 1;
    }

    return true;
  }

  connected(a, b) {
    return this.find(a) === this.find(b);
  }
}

class Solution {
  validTree(n, edges) {
    if (edges.length !== n - 1) return false;

    const ds = new DisjointSet(n);

    for (const [u, v] of edges) {
      if (!ds.union(u, v)) {
        return false;
      }
    }

    return true;
  }
}
```
