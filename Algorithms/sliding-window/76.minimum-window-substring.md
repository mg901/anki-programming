## [76 Minimum Window Substring](https://leetcode.com/problems/minimum-window-substring/description/)

<!-- notecardId: 1741280664407 -->

```js
function minWindow(s, t) {
  if (s.length < t.length) return "";

  let charFreq = new Map();

  for (let char of t) {
    charFreq.set(char, (charFreq.get(char) ?? 0) + 1);
  }

  let toCover = t.length;
  let minLength = Infinity;
  let windowStart = 0;
  let left = 0;

  for (let right = 0; right < s.length; right += 1) {
    const char = s[right];

    if (charFreq.has(char)) {
      if (charFreq.get(char) > 0) toCover -= 1;
      charFreq.set(char, charFreq.get(char) - 1);
    }

    while (toCover === 0) {
      const windowLength = right - left + 1;

      if (windowLength < minLength) {
        minLength = windowLength;
        windowStart = left;
      }

      const leftChar = s[left];

      if (charFreq.has(leftChar)) {
        charFreq.set(leftChar, charFreq.get(leftChar) + 1);
        if (charFreq.get(leftChar) > 0) toCover += 1;
      }

      left += 1;
    }
  }

  return minLength === Infinity
    ? ""
    : s.slice(windowStart, windowStart + minLength);
}
```
